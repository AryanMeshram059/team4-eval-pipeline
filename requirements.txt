# TEAM 4 Evaluation Pipeline - Requirements
# ============================================
# Core dependencies for data evaluation, mathematical verification,
# code evaluation, and AI-powered reasoning critique.

# ── Python Version ──────────────────────────────────────────────────
# Minimum: Python 3.8
# Recommended: Python 3.10 or higher

# ── Data Processing & Utilities ─────────────────────────────────────
jsonl>=1.6.3
pandas>=1.3.0
numpy>=1.21.0
tqdm>=4.62.0

# ── Mathematical Verification ──────────────────────────────────────
math-verify>=0.1.0  # HuggingFace library for symbolic math comparison
sympy>=1.12         # Required by math-verify for symbolic manipulation

# ── Code Evaluation ────────────────────────────────────────────────
# LiveCodeBench integration (included in tools/ folder)
# APPS Metric evaluation library
datasets>=2.10.0    # For APPS dataset loading
timeout-decorator>=0.5.0  # For safe code execution with timeouts

# ── AI Models & Transformers ────────────────────────────────────────
torch>=1.13.0       # PyTorch for model inference
transformers>=4.30.0  # HuggingFace transformers for Shepherd model
accelerate>=0.20.0  # For efficient model loading/inference
bitsandbytes>=0.41.0  # For 8-bit quantization (optional, for Shepherd)

# ── Safety & Security ──────────────────────────────────────────────
ast>=3.9           # Built-in, for AST-based code analysis
regex>=2023.0.0    # For advanced pattern matching

# ── Logging & Reporting ────────────────────────────────────────────
python-dateutil>=2.8.0  # For timestamp handling

# ── Development & Testing (Optional) ────────────────────────────────
pytest>=7.0.0
black>=22.0.0       # Code formatting
flake8>=4.0.0       # Linting
mypy>=0.990         # Type checking

# ── Parquet Support (Optional, for data export) ────────────────────
pyarrow>=11.0.0
fastparquet>=0.8.0
